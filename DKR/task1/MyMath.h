#ifndef _MYMATH_H
#define _MyMATH_H 1

#endif
/**
 * @brief Function for raising number to power
 * @param base_number number which is raised to power
 * @param power power to we which we raising base
 */
double mypow(double base_number, int power);

/// @brief Function to take factorial of a number. Return factorial of a number, 0 if operation goes wrong
long int myfactorial(int number);